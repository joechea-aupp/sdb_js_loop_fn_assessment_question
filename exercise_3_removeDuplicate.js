/**
 * Create a function named removeDuplicates that takes one parameter:

- arr (an array of integers) - This parameter represents the input array from which duplicate numbers are to be removed.

Inside the function:

- Create an empty object named seen.
- Create an empty array named result.
- Loop through each element of the input array arr.
- For each element:
    - Check if the element exists as a key in the seen object. If it doesn't, add it to the result array and set its value in the seen object to true.
    - Return the result array.

Example:

Input:

removeDuplicates([1, 2, 2, 3, 4, 4, 5]);

Output:
[1, 2, 3, 4, 5]
 */

function removeDuplicate(arr) {
  // your answer here!
}

module.exports = removeDuplicate;
